{
        $builder
            ->add('email', EmailType::class)
            ->add('plainPassword', RepeatedType::class, [
                'type' => PasswordType::class,
                'first_options' => ['label' => 'Password'],
                'second_options' => ['label' => 'Repeat Password'],
                'mapped' => false,
                'attr' => ['autocomplete' => 'new-password'],
            ]);
    }
=======
    public function buildForm(FormBuilderInterface $builder, array $options): void
    {
        $builder
            ->add('email', EmailType::class)
            ->add('plainPassword', RepeatedType::class, [
                'type' => PasswordType::class,
                'first_options' => ['label' => 'Password'],
                'second_options' => ['label' => 'Repeat Password'],
                'mapped' => false,
                'attr' => ['autocomplete' => 'new-password'],
            ])
            ->add('name', TextType::class, [
                'required' => true,
            ])
            ->add('countryCode', ChoiceType::class, [
                'choices' => $this->getCountryCodeChoices(),
                'placeholder' => 'Select Country Code',
                'required' => false,
            ])
            ->add('mobileNumber', TextType::class, [
                'required' => false,
                'label' => 'Mobile Number',
            ])
            ->add('organizationName', TextType::class, [
                'required' => false,
                'label' => 'Organization Name',
            ])
            ->add('jobRole', ChoiceType::class, [
                'choices' => [
                    'Student' => 'Student',
                    'Security Researcher' => 'Security Researcher',
                    'Penetration Tester' => 'Penetration Tester',
                    'Incident Responder' => 'Incident Responder',
                    'Security Analyst' => 'Security Analyst',
                    'Other' => 'Other',
                ],
                'placeholder' => 'Select Job Role',
                'required' => false,
                'label' => 'Job Role',
            ])
            ->add('jobDescription', TextareaType::class, [
                'required' => false,
                'label' => 'Describe your job',
            ]);

        // Add event listener to clear jobDescription if jobRole is not 'Other'
        $builder->addEventListener(FormEvents::PRE_SUBMIT, function (FormEvent $event) {
            $data = $event->getData();
            if (isset($data['jobRole']) && $data['jobRole'] !== 'Other') {
                $data['jobDescription'] = null;
                $event->setData($data);
            }
        });
    }
>>>>>>> REPLACE

<<<<<<< SEARCH
    public function configureOptions(OptionsResolver $resolver): void
    {
        $resolver->setDefaults([
            'data_class' => User::class,
        ]);
    }
}
=======
    public function configureOptions(OptionsResolver $resolver): void
    {
        $resolver->setDefaults([
            'data_class' => User::class,
        ]);
    }

    private function getCountryCodeChoices(): array
    {
        return [
            '+1 (US)' => '+1',
            '+44 (UK)' => '+44',
            '+91 (IN)' => '+91',
            '+61 (AU)' => '+61',
            '+81 (JP)' => '+81',
            '+49 (DE)' => '+49',
            '+33 (FR)' => '+33',
            '+86 (CN)' => '+86',
            '+7 (RU)' => '+7',
            '+55 (BR)' => '+55',
        ];
    }
}

{{ form_row(registrationForm.email) }}
                        {{ form_row(registrationForm.plainPassword.first) }}
                        {{ form_row(registrationForm.plainPassword.second) }}

                        <div class="d-grid">
                            <button class="btn btn-lg btn-primary" type="submit">
                                Register
                            </button>
                        </div>
                    {{ form_end(registrationForm) }}
=======
                    {{ form_start(registrationForm) }}
                        {{ form_row(registrationForm.email) }}
                        {{ form_row(registrationForm.plainPassword.first) }}
                        {{ form_row(registrationForm.plainPassword.second) }}

                        {{ form_row(registrationForm.name) }}
                        {{ form_row(registrationForm.countryCode) }}
                        {{ form_row(registrationForm.mobileNumber) }}
                        {{ form_row(registrationForm.organizationName) }}
                        {{ form_row(registrationForm.jobRole) }}
                        <div id="job-description-wrapper" style="display: none;">
                            {{ form_row(registrationForm.jobDescription) }}
                        </div>

                        <div class="d-grid">
                            <button class="btn btn-lg btn-primary" type="submit">
                                Register
                            </button>
                        </div>
                    {{ form_end(registrationForm) }}
>>>>>>> REPLACE

<<<<<<< SEARCH
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
=======
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function toggleJobDescription() {
        const jobRoleSelect = document.getElementById('{{ registrationForm.jobRole.vars.id }}');
        const jobDescriptionWrapper = document.getElementById('job-description-wrapper');
        if (jobRoleSelect.value === 'Other') {
            jobDescriptionWrapper.style.display = 'block';
        } else {
            jobDescriptionWrapper.style.display = 'none';
            // Clear job description input when hidden
            const jobDescriptionInput = document.getElementById('{{ registrationForm.jobDescription.vars.id }}');
            if (jobDescriptionInput) {
                jobDescriptionInput.value = '';
            }
        }
    }

    document.addEventListener('DOMContentLoaded', function () {
        const jobRoleSelect = document.getElementById('{{ registrationForm.jobRole.vars.id }}');
        if (jobRoleSelect) {
            toggleJobDescription(); // Initial toggle based on current value
            jobRoleSelect.addEventListener('change', toggleJobDescription);
        }
    });
</script>

{% endblock %}
